# FSV9563 RFID 模块调试记录

## 项目背景

| 项目 | 说明 |
|------|------|
| 模块 | 自制 FSV9563 RFID 模块（STM32F103C8 + FSV9563 芯片） |
| 控制板 | Fully Test 控制板（STM32 + µC/OS-II RTOS） |
| 通讯方式 | UART 115200bps，自定义帧协议 |
| 问题现象 | 控制板与 FSV9563 模块通讯异常，所有指令均无法正常响应 |

### 帧协议格式

```
发送帧：[LEN][ADDR][CMD][DATA...][BCC]
响应帧：[LEN][ADDR][CMD][SW][DATA...][BCC]

BCC = (~sum_of_all_bytes_before_BCC) & 0xFF
LEN = 帧总字节数（含LEN自身）
SW  = 0x00 成功，非0 失败
```

### 指令列表

| CMD | 功能 |
|-----|------|
| 0x15 | GetInfo — 获取模块版本信息 |
| 0x01 | SelectChannel — 选择天线通道（1/3/5/7/9/11） |
| 0x11 | GetAddr — 查询模块地址 |
| 0x12 | SetAddr — 修改模块地址 |
| 0x02 | AutoScan — 扫描全部通道，返回有卡通道和UID |
| 0x21 | ReadBlock — 读单块（16字节） |
| 0x22 | WriteBlock — 写单块（16字节） |
| 0x25 | ReadSector — 读扇区（48字节，块0/1/2） |
| 0x27 | WriteSector — 写扇区（48字节，块0/1/2） |

---

## Bug 列表与修复过程

---

### Bug 1 — UART 字节丢失（通讯根基问题）

**文件：** `main.c`

**现象：** 收到第一个字节后，主循环进入 SPI 操作（数毫秒），期间 UART 中断继续到来，新字节覆盖全局变量，后续字节全部丢失。

**根因：** 原实现使用单字节全局变量 `g_rxData`/`g_rxReady`，中断写入、主循环读取，无缓冲机制。

**修复：** 改为 128 字节环形缓冲区，中断写入，主循环逐字节消费。

```c
// main.c — 修复后
#define UART_RX_BUF_SIZE  128
volatile u8 g_rxBuf[UART_RX_BUF_SIZE];
volatile u8 g_rxHead = 0;
volatile u8 g_rxTail = 0;

// 中断：
void USART1_IRQHandler(void) {
    u8 data = (u8)USART_ReceiveData(USART1);
    u8 nextHead = (g_rxHead + 1) % UART_RX_BUF_SIZE;
    if(nextHead != g_rxTail) {
        g_rxBuf[g_rxHead] = data;
        g_rxHead = nextHead;
    }
}

// 主循环：
while(g_rxHead != g_rxTail) {
    u8 data = g_rxBuf[g_rxTail];
    g_rxTail = (g_rxTail + 1) % UART_RX_BUF_SIZE;
    RFID_RespondAnalysis(data);
}
```

---

### Bug 2 — PA15/PB3 GPIO 无法拉低（通道切换失效）

**文件：** `FSV9563.C`（`FSV9563_Init`，第 137 行附近）

**现象：** 调用 `GPIO_ResetBits(GPIOA, GPIO_Pin_15)` 后 PA15 始终保持高电平，通道 2/4/6 无法正常切换。

**根因：** PA15 = JTAG TDI 引脚，PB3 = JTAG TDO 引脚。STM32 上电后 JTAG 接口默认使能，锁定这两个引脚，用户代码无法控制。

**修复：** 在任何 GPIO 初始化之前，先使能 AFIO 时钟并禁用 JTAG（仅保留 SWD）：

```c
// FSV9563.C — FSV9563_Init 开头
RCC_APB2PeriphClockCmd(
    RCC_APB2Periph_GPIOA | RCC_APB2Periph_GPIOB |
    RCC_APB2Periph_ADC1  | RCC_APB2Periph_AFIO, ENABLE);

GPIO_PinRemapConfig(GPIO_Remap_SWJ_JTAGDisable, ENABLE);  // 禁用JTAG，释放PA15/PB3
```

**通道 GPIO 对应关系：**

| 内部通道 | 外部通道号 | PA5 | PA1 | PA15 | PB3 |
|----------|------------|-----|-----|------|-----|
| 0 | 1  | 1 | 0 | 0 | 0 |
| 1 | 3  | 1 | 0 | 1 | 0 |
| 2 | 5  | 1 | 0 | 0 | 1 |
| 3 | 7  | 0 | 1 | 0 | 0 |
| 4 | 9  | 0 | 1 | 1 | 0 |
| 5 | 11 | 0 | 1 | 0 | 1 |

---

### Bug 3 — GetAddr (0x11) 响应地址字段错误

**文件：** `RFID_Compat.c`（`RFID_Cmd_GetAddr`）

**现象：** 控制板查询模块地址后超时，无法获取地址。

**根因：** 控制板的响应解析器在接收流中搜索 `S_RFID_ModuleAdr`（0x01）字节作为帧头定位。原实现用 `addr=0x00` 发响应，控制板永远无法定位帧头。

**修复：**

```c
// 改前
RFID_SendResponse(0x00, 0x11, 0x00, resp, 1);

// 改后
RFID_SendResponse(S_RFID_ModuleAdr, 0x11, 0x00, resp, 1);
```

---

### Bug 4 — SetAddr (0x12) 响应时序错误

**文件：** `RFID_Compat.c`（`RFID_Cmd_SetAddr`）

**现象：** 控制板修改模块地址后收不到成功响应。

**根因：** 原实现先更新 `S_RFID_ModuleAdr`，再用**新地址**发响应。但控制板此时还持有**旧地址**作为匹配标识，新地址帧头无法被识别。

**修复：** 先用旧地址发响应，控制板收到后更新自己的记录，FSV9563 再更新本地地址：

```c
// 改前
S_RFID_ModuleAdr = newAdr;
RFID_SendResponse(S_RFID_ModuleAdr, 0x12, 0x00, NULL, 0);  // 用新地址发，控制板找不到

// 改后
RFID_SendResponse(S_RFID_ModuleAdr, 0x12, 0x00, NULL, 0);  // 先用旧地址响应
S_RFID_ModuleAdr = newAdr;                                   // 再更新地址
```

---

### Bug 5 — AutoScan (0x02) 通道掩码解析错误

**文件：** `RFID_Compat.c`（`RFID_Cmd_AutoScan`）

**现象：** 发送 `06 01 02 55 15 8C`，卡贴近天线仍返回无卡 `05 01 02 03 F4`。

**根因：** 控制板发送 2 字节掩码 `0x55 0x15`，编码规则为 `bit(外部通道号-1)=1` 表示扫描该通道。原代码只读 1 个字节且直接用 bit 索引映射，导致：
- `0x55` 的 bit0/2/4 = 1 → 只扫内部通道 0/2/4（外部 1/5/9）
- `0x15` 第二字节完全被忽略
- 内部通道 1/3/5（外部 3/7/11）永远不被扫描

**正确编码规则：**

| 掩码字节 | Bit | 内部通道 | 外部通道 |
|----------|-----|----------|----------|
| mask1 (rxBuf[3]) | bit0 | 0 | 1 |
| mask1 | bit2 | 1 | 3 |
| mask1 | bit4 | 2 | 5 |
| mask1 | bit6 | 3 | 7 |
| mask2 (rxBuf[4]) | bit0 | 4 | 9 |
| mask2 | bit2 | 5 | 11 |

**修复：**

```c
u8 mask1 = rxBuf[3];  // 0x55
u8 mask2 = rxBuf[4];  // 0x15
for(i = 0; i < 6; i++) {
    u8 mByte = (i < 4) ? mask1 : mask2;
    u8 mBit  = (i < 4) ? (u8)(i * 2) : (u8)((i - 4) * 2);
    if((mByte & (1 << mBit)) == 0) continue;
    // ... 扫描该通道
}
```

---

### Bug 6 — AutoScan 有卡误判为无卡

**文件：** `RFID_Compat.c`（`RFID_Cmd_AutoScan`）

**现象：** 即使卡片激活成功，AutoScan 仍报无卡。

**根因：** `FSV9563_SingleChannel_Read` 在激活卡后还会尝试鉴权和读块，若默认密钥不匹配则返回非 `MI_OK`。原代码要求 `status == MI_OK && card_type == 1` 才算有卡，鉴权失败时 `card_type` 已置 1 但被忽略。

**修复：** AutoScan 只需判断卡片是否存在，不关心数据读取结果：

```c
// 改前
if(status == MI_OK && card_type == 1)

// 改后
FSV9563_SingleChannel_Read(i, &card_type, uid, &uid_len, card_data);
if(card_type == 1)  // 卡已激活即视为存在
```

---

### Bug 7 — WriteSector 帧被拒收

**文件：** `RFID_Compat.c`（`RFID_RespondAnalysis`）

**现象：** WriteSector 命令发出后 FSV9563 无响应。

**根因：** 控制板 WriteSector 帧长 = 61 字节（LEN=0x3D），原帧解析器上限为 60，帧头被丢弃。

**修复：**

```c
// 改前
if(data >= 4 && data <= 60)

// 改后
if(data >= 4 && data <= 63)  // UART_RxBuf[64] 最大容纳 63 字节帧
```

---

### Bug 8 — ReadBlock/ReadSector 忽略 sector/block/key 参数

**文件：** `RFID_Compat.c`（`RFID_Cmd_ReadBlock`、`RFID_Cmd_ReadSector`）

**现象：** 读操作不论指定哪个扇区/块，始终返回相同数据（扇区0块1）。

**根因：** 原实现调用 `FSV9563_SingleChannel_Read`，该函数内部硬编码：默认密钥 FF×6、扇区0鉴权、读块1。控制板传来的 sector/block/key 参数全部被忽略。

另有两个附带问题：
- ReadSector 声明 `card_data[48]` 但只填充 16 字节，响应中后 32 字节为栈随机数据
- 冗余的 PDOWN + spin delay（`FSV9563_SingleChannel_Read` 内部已有精确 delay_ms 版本）

**修复：** 重写为直接调用底层函数，使用控制板传入的参数：

```c
// RFID_Cmd_ReadBlock 核心流程
blockAddr = sectorIdx * 4 + blockIdx;
RFID_ActivateCard(uid);                          // 激活卡，获取UID
FSV9563_CMD_LoadKey(key);                        // 装载密钥
FSV9563_CMD_MfcAuthenticate(0x60, sectorIdx*4, uid);  // 鉴权
FSV9563_PcdMfcRead(blockAddr, card_data);        // 读目标块
```

```c
// RFID_Cmd_ReadSector 核心流程（读 block 0/1/2，共48字节）
RFID_ActivateCard(uid);
FSV9563_CMD_LoadKey(key);
FSV9563_CMD_MfcAuthenticate(0x60, sectorIdx*4, uid);
for(b = 0; b < 3; b++)
    FSV9563_PcdMfcRead(sectorIdx*4 + b, &card_data[b*16]);
```

---

### Bug 9 — WriteBlock/WriteSector 为空存根，不实际写卡

**文件：** `RFID_Compat.c`（`RFID_Cmd_WriteBlock`、`RFID_Cmd_WriteSector`）

**现象：** 写操作控制板收到成功响应，但卡片数据未改变。

**根因：** 两个函数仅做参数校验后直接返回 SW=0x00，未执行任何 SPI 写操作。

**修复：** 实现真实写流程：

```c
// RFID_Cmd_WriteBlock 核心流程
RFID_ActivateCard(uid);
FSV9563_CMD_LoadKey(key);
FSV9563_CMD_MfcAuthenticate(0x60, sectorIdx*4, uid);
FSV9563_PcdMfcWrite(blockAddr, &rxBuf[11]);      // 数据在帧的 [11..26]

// RFID_Cmd_WriteSector 核心流程（写 block 0/1/2）
RFID_ActivateCard(uid);
FSV9563_CMD_LoadKey(key);
FSV9563_CMD_MfcAuthenticate(0x60, sectorIdx*4, uid);
for(b = 0; b < 3; b++)
    FSV9563_PcdMfcWrite(sectorIdx*4 + b, &rxBuf[12 + b*16]);  // 数据从帧 [12] 起
```

---

### Bug 10 — 鉴权函数架构不匹配（所有读写失败的根因）

**文件：** `RFID_Compat.c`（ReadBlock/WriteBlock/ReadSector/WriteSector）

**现象：** 编译烧录后，6个通道读写全部失败，上位机报写失败/读失败。使用商用 RFID 模块正常。

**根因：** FSV9563 是 **CLRC663 架构**芯片，Mifare 鉴权为**两步协议**：

```c
// FSV9563.h 命令定义
#define FSV9563_LoadKey   0x02  // 6字节密钥 → 写入芯片密钥缓冲区
#define FSV9563_MFAuthent 0x03  // mode(1)+block(1)+UID(4) = 6字节，用缓冲区密钥鉴权
```

而代码使用的是 `FSV9563_PcdAuthState`，该函数是 **MFRC522 架构**风格，一次发送 **12 字节**（mode+block+key×6+UID×4），直接嵌入密钥：

```
FSV9563 芯片期望：LoadKey(6字节) + MFAuthent(6字节)  → 鉴权成功
实际发送：MFAuthent(12字节，MFRC522格式)           → 密钥缓冲区为空 → 鉴权必然失败
```

另一佐证 — 两个函数检查的状态位不同：

| 函数 | 检查位 | 含义 |
|------|--------|------|
| `FSV9563_CMD_MfcAuthenticate`（正确） | `rRegStatus & 0x20`（bit5） | CLRC663 CryptoOn |
| `FSV9563_PcdAuthState`（错误） | `rRegStatus & 0x08`（bit3） | MFRC522 Crypto1On，在FSV9563无意义 |

**修复：** 将全部4处鉴权从：

```c
// 错误（MFRC522风格，12字节，不适用于FSV9563）
status = FSV9563_PcdAuthState(0x60, sectorIdx*4, key, uid);
```

改为：

```c
// 正确（CLRC663风格，两步鉴权）
status = FSV9563_CMD_LoadKey(key);
if(status != MI_OK) { /* 返回错误 */ }
status = FSV9563_CMD_MfcAuthenticate(0x60, sectorIdx*4, uid);
if(status != MI_OK) { /* 返回错误 */ }
```

同时在 `FSV9563.h` 补充导出声明：

```c
s8 FSV9563_CMD_LoadKey(u8 *pkey);
s8 FSV9563_CMD_MfcAuthenticate(u8 auth_mode, u8 block, u8 *pSnr);
```

---

## 修改文件汇总

| 文件 | 修改内容 |
|------|----------|
| `main.c` | UART 单字节变量 → 128字节环形缓冲区 |
| `FSV9563.C` | 添加 AFIO 时钟 + JTAG 禁用（释放 PA15/PB3） |
| `FSV9563.h` | 导出 `FSV9563_PcdActivateA`、`FSV9563_PcdMfcRead`、`FSV9563_PcdMfcWrite`、`FSV9563_CMD_LoadKey`、`FSV9563_CMD_MfcAuthenticate` |
| `RFID_Compat.c` | 详见下表 |

### RFID_Compat.c 改动明细

| 函数 | 改动 |
|------|------|
| `RFID_RespondAnalysis` | 帧长上限 60 → 63 |
| `RFID_Cmd_GetAddr` | 响应 addr 0x00 → S_RFID_ModuleAdr |
| `RFID_Cmd_SetAddr` | 先响应再改地址 |
| `RFID_Cmd_AutoScan` | 双字节掩码偶数位映射；card_type==1 即判有卡；删除冗余 PDOWN |
| `RFID_Cmd_ReadBlock` | 重写：ActivateCard + LoadKey + MfcAuthenticate + MfcRead（使用控制板传入参数）|
| `RFID_Cmd_WriteBlock` | 重写：由存根改为真实写操作 |
| `RFID_Cmd_ReadSector` | 重写：循环读3块返回48字节真实数据；修复垃圾数据问题 |
| `RFID_Cmd_WriteSector` | 重写：由存根改为真实写操作 |
| 新增 `RFID_ActivateCard` | 静态辅助函数：通道切换 + PDOWN 复位 + ConfigISOType + PcdActivateA |

---

## 最终指令响应状态

| CMD | 功能 | 最终状态 |
|-----|------|----------|
| 0x15 | GetInfo | ✅ 正常 |
| 0x01 | SelectChannel | ✅ 正常 |
| 0x11 | GetAddr | ✅ 修复（Bug 3） |
| 0x12 | SetAddr | ✅ 修复（Bug 4） |
| 0x02 | AutoScan | ✅ 修复（Bug 5/6） |
| 0x21 | ReadBlock | ✅ 修复（Bug 7/8/10） |
| 0x22 | WriteBlock | ✅ 修复（Bug 7/9/10） |
| 0x25 | ReadSector | ✅ 修复（Bug 7/8/10） |
| 0x27 | WriteSector | ✅ 修复（Bug 7/9/10） |
